# demo-validation

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build demo-validation` to build the library.

## Running unit tests

Run `nx test demo-validation` to execute the unit tests via [Jest](https://jestjs.io).
